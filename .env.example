# Copy this file to .env.local and fill in your values.
# .env.local is gitignored — never commit it.

# ── Required ──────────────────────────────────────────────────────────────────
# Target app URL for all tests. Use staging, never production.
BASE_URL=https://your-staging-app.com

# ── Auth (required for auth-protected tests) ──────────────────────────────────
TEST_USER_EMAIL=qa@your-app.com
TEST_USER_PASSWORD=

# ── API (optional — used by API tests that call authenticated endpoints) ───────
API_KEY=

# ── CI notifications (optional) ───────────────────────────────────────────────
# Slack incoming webhook — https://api.slack.com/messaging/webhooks
SLACK_WEBHOOK_URL=

# Lighthouse CI GitHub App token — https://github.com/apps/lighthouse-ci
LHCI_GITHUB_APP_TOKEN=
